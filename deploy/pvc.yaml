apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tf2-hosting-data
  labels:
    app: tf2-hosting-app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      # Holds the SQLite metadata DB (users, and later requests/servers). Small.
      storage: 1Gi
